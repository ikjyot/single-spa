// 1. Export the CSS (which includes Tailwind v4 and Shadcn CSS variables)
import './index.css';

// 2. Export the generated Button component
export { Button, buttonVariants } from './components/ui/button';